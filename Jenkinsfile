pipeline {
   agent {label 'jenkins'}
      stages {
         stage('Setting the variables values') {
              bash '''#!/bin/bash
                      echo "hello world" 
         '''
    }
  }
}
